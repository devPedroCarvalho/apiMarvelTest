package test.test.apimarveltest.view.listCharacter

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class ViewHolder constructor(itemView: View) : RecyclerView.ViewHolder(itemView) {

    /*
private val recyclerView = itemView.rvGoals
private val typeOfGoal = itemView.txt_type_goal
private val date = itemView.txt_date_model
private val value = itemView.txt_value
private val name = itemView.txt_employee_name

fun bind(goals: Goals){
    typeOfGoal.text = goals.typeGoals
    date.text = goals.data
    value.text = goals.valor
    name.text = goals.nomeDoFuncionario
}

 */

}